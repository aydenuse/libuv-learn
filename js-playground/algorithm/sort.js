'use strict';

const arr = [5, 2, 3, 1, 4, 7, 9, 8];
const sort = (lboundry = 0, rboundry = arr.length - 1, arr) => {

    let lindex = lboundry;
    let rindex = rboundry;
    let pivot = lboundry;

    while (lindex < rindex) {

        while (lindex < rindex) {
            
        }

    }

}